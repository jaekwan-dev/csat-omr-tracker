# 🏫 교사용 매뉴얼 (Teacher Manual)

본 매뉴얼은 교사 및 관리자를 위한 수능 OMR 성적 관리 플랫폼 사용 안내서입니다.

---

## 1. 교사 로그인 (Teacher Login)
- **접속 경로**: `/teacher/login`
- **화면 구성**: 입력에 집중할 수 있는 깔끔한 단일 카드 폼 형태로 구성되어 있습니다.
- **로그인 정보**: 교사 전용 비밀번호 입력 (환경 변수로 설정된 `TEACHER_PASSWORD`)
- 로그인 성공 시 관리자 전용 JWT 쿠키가 발급되어 성적 대시보드로 이동합니다.

---

## 2. 학생 관리 (Student Management)
- **접속 경로**: `/teacher/students`
- **모바일 최적화 전용 카드 UI**: 모바일 화면에서는 좁은 가로 스크롤 테이블 대신 터치 친화적인 **학생 카드 뷰**(이니셜 아바타, 학번, 학년/반 칩, PIN 번호 마스킹 토글, 제출 건수 칩, 큰 터치 영역의 수정/삭제 버튼)로 자동 변환됩니다.
- **개별 등록**:
  - 학번, 이름, 학년, 반 정보를 입력하여 학생 계정을 생성합니다.
  - 학생 생성 시 대리 마킹 방지를 위한 **고유 PIN 번호(4자리)**가 자동 발급됩니다.
- **CSV 대량 등록**:
  - 엑셀/CSV 양식을 통해 수십 명 단위의 학생 목록을 일괄 등록할 수 있습니다.
  - 기존 등록 학생의 PIN 번호도 일괄 업로드 및 갱신이 지원됩니다.

---

## 3. 시험 등록 및 관리 (Exam Management)
- **접속 경로**: `/teacher/exams`
- **상단 3열 과목 그리드 필터바 & 컴팩트 리스트 뷰**:
  - `전체 과목`을 제거하고 `국어 📚`, `수학 ✏️`, `영어 💡` 3개 과목을 3열 균등 그리드로 배치하여 스크롤 없이 한눈에 선택할 수 있습니다.
  - 시험 목록을 한 줄씩 깔끔하게 정돈된 **컴팩트 리스트 형태**(과목 뱃지, 시험 제목, 문항 수, 만점, 제출 건수 칩, 정답 수정/삭제 버튼)로 변환하여 수십 개의 시험을 한눈에 조회 및 관리할 수 있습니다.
- **신규 시험 등록**:
  - 과목(국어/수학/영어), 모의고사 제목, 전체 문항 수, 시작 번호를 설정하여 등록합니다.
  - 수학 영역의 경우 단답형 주관식 문항 지정이 지원됩니다.
- **정답 및 배점 수정**:
  - 등록된 시험의 `✏️ 정답 수정` 버튼을 통해 정답표와 문항별 배점(1~10점)을 모달에서 즉시 수정할 수 있습니다.

---

## 4. 성적 대시보드 (Dashboard)
- **접속 경로**: `/teacher`
- **화면 구성 순서**:
  1. **스마트 필터 패널**: 과목 선택(3열 그리드) → 시험 회차 선택(카드 그리드) → 학반 선택(드롭다운)
  2. **요약 지표 카드**: 선택 시험, 응시자 수, 평균 점수, 최고 점수 KPI
  3. **📉 문항별 정답률 통계**: 시험 전체의 문항별 정답/오답률과 최다 오답 문항 분석이 상단에 바로 노출됩니다.
  4. **📊 학생별 성적 리스트**: 학생별 석차, 학번, 총점, 정답률 및 터치 시 펼쳐지는 개인 성적 추이 그래프를 조회합니다.
